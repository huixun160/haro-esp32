# Rule: Spec Freeze

After all clarification questions are answered, AI must generate a frozen specification before implementation begins.

The frozen spec must classify all requirements as:
- **MUST** — Required for task completion
- **SHOULD** — Expected but not blocking
- **MAY** — Optional, nice to have
- **OUT OF SCOPE** — Explicitly excluded

The engineer must review and approve the frozen spec before implementation proceeds.
