# TM-029-2.5 Feedback — Voice Assistant Stabilization

**Outcome:** BLOCKED
**Date:** 2026-03-15 ~ 2026-03-16

---

## Completed Work

1. **WiFi status constant fix** — `bs_network.c` status 4→2 (Logel verified)
2. **Key callback ABI fix** — `dap_lvgl_bridge.c` + `mmi_lvgl_win.c` 2-param dispatch
3. **Recording re-entry guard** — `dap_audio_bridge.c` `s_rec_active` check
4. **MMI thread blocking fix** — `bs_task.c` removed `SCI_Sleep` loop
5. **Architecture analysis** — `AIOS/docs/architecture/voice_assistant_architecture.md`

## Incomplete Work

- Voice assistant end-to-end flow (record → server → audio response) not verified
- DSP crash on WiFi-connected startup not resolved
- UI state machine accuracy ("就绪" shown before WS connection) not addressed

## Blocking Issues

**DSP assert `dspintisr.c:587`** on opening voice assistant with WiFi connected. This is a platform-level audio hardware conflict that cannot be fixed by patching application code. The contaminated snapshot's `dap_audio_bridge.c` (60KB) has struct layouts and DSP interactions that may be incompatible with the TM28R SDK.

## Verification Status

| Test | Result |
|------|--------|
| WiFi detection | ✅ Confirmed via Logel (`BS_CORE_TASK >>> INIT` without "no WiFi") |
| Enqueue crash fix | ✅ No more `sdi_msg_iram.c` crash |
| DSP stability | ❌ New DSP assert when WiFi connected |
| Voice response | ❌ Not reached (blocked by DSP crash) |

## Next Actions

1. **Version rollback** — Consider reverting to a clean baseline without Ouyang's contaminated snapshot
2. **If continuing VA integration** — need access to Ouyang's working device/PAC to compare DSP configuration
3. **Architecture doc** available at `AIOS/docs/architecture/voice_assistant_architecture.md` for future reference
